[package]
name = "api_tests"
version = "0.1.0"
edition = "2021"

[dependencies]
server = { path = "../server", package = "server" }
models = { path = "../models", package = "models" }
approx = "0.5"
csv = "1.3"